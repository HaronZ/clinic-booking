# PHP
/backend/vendor/
/backend/.env
/backend/.env.testing
/backend/.phpunit.result.cache
# composer.lock is committed — ensures Railway installs the exact same versions

# Angular / Node
/frontend/node_modules/
/frontend/dist/
/frontend/.angular/
# package-lock.json is committed — ensures Railway installs the exact same versions

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini
